trigger:
- main

pr: none

pool:
  vmImage: ubuntu-latest

steps:

- checkout: self

- script: |
    echo "Current Directory"
    pwd

    echo ""
    echo "Files"
    ls -la

    echo ""
    echo "Recursive Files"
    ls -R
  displayName: Explore Agent
